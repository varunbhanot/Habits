spring:
  application:
    name: registry
  cloud:
    config:
      enabled: false
eureka:
  instance:
    prefer-ip-address: true
  client:
    registerWithEureka: false
    fetchRegistry: false
    
server:
  port: 8761    