spring:
  application:
    name: registry
    
  cloud:
    config:
      uri: http://localhost:8888
      fail-fast: true
eureka:
  instance:
    prefer-ip-address: true
  client:
    registerWithEureka: false
    fetchRegistry: false   