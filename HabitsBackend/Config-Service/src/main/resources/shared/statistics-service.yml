server:
  port: 7000
  context-path: /stats
  jpa:
    hibernate:
      ddl-auto: none
  datasource:
    url: jdbc:mysql://statistics-db:3306/stats
    username: app_user
    password: ${STATS_DB_PASSWORD}
flyway:
  enabled: true
  locations:
  - classpath:db/mysql
  schemas:
  - stats